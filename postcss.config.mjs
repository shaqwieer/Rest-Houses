/** Tailwind CSS v4 is a PostCSS plugin — no tailwind.config.js needed.
 *  The design tokens live in src/app/globals.css under `@theme`. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
